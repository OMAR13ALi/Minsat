server.port=5000

# MySQL — same database as Express backend
spring.datasource.url=jdbc:mysql://localhost:3307/minsat?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.hikari.maximum-pool-size=10

# UCIP wrapper
ucip.base-url=http://localhost:8080

# Voucher Admin XML-RPC
voucher.url=http://10.13.0.49:10020/VoucherAdmin
voucher.auth=Basic bmd2c3VzZXI6VmVudGVAQElOQEAyMDIz

# HLR Telnet
hlr.host=10.23.69.209
hlr.port=7776
hlr.login-cmd=LGI:OPNAME="mmlgwin",PWD="Orange@Huawei@2024";
hlr.timeout-ms=5000

# JWT
jwt.secret-fallback=OrangeMinsat2025
jwt.expiration-days=7
jwt.reset-expiration-hours=1

# Vault (KV v2 — fetches jwt_secret, mail_user, mail_pass at startup)
vault.uri=http://minsat-vault:8200
vault.token=dev-root-token
vault.kv-path=secret/data/myapp/minsat

# Mail (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=maysiwar96@gmail.com
spring.mail.password=oiefqkytotllwurq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Frontend base URL (for reset/activation links in emails)
app.frontend-url=http://localhost:5173

# Audit log (file-based — replaces system_logs DB table)
audit.log.dir=./audit-logs
audit.log.max-size-mb=10
audit.log.retention-days=30

# Serialize LocalDateTime as ISO-8601 string so the frontend Date() can parse it
spring.jackson.serialization.write-dates-as-timestamps=false

# Logging
logging.level.com.minsat=DEBUG
